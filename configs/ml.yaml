host: 0.0.0.0
port: 8003

model_dir: /app/models

model_name: ocsvm_text.joblib

tfidf:
  max_features: 5000
  ngram_range: [1, 1]
  min_df: 1
  max_df: 1.0
  lowercase: false
  token_pattern: "(?u)\\b\\w+\\b"

ocsvm:
  kernel: rbf
  gamma: scale
  nu: 0.05

